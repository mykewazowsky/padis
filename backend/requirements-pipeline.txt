# =============================================================================
# PADIS Pipeline — Python dependencies (pipeline only, no Flask/web layer)
# Digunakan oleh Dockerfile.pipeline.
# =============================================================================

# --- Geospatial core ---
# Semua paket ini menyertakan binary GDAL/GEOS/PROJ bundled dalam manylinux
# wheel, sehingga tidak membutuhkan instalasi system GDAL terpisah.
geopandas==1.1.3
rasterio==1.4.3
fiona==1.10.1
shapely==2.1.2
pyogrio==0.12.1
pyproj==3.7.2
affine==2.4.0

# --- Zonal statistics ---
# CATATAN: rasterstats tidak ada di requirements.txt utama — sudah ditambahkan.
rasterstats==0.20.0

# --- Data processing ---
numpy==2.4.3
pandas==3.0.1
python-dateutil==2.9.0.post0
tzdata==2025.3

# --- Database ---
psycopg2-binary==2.9.9

# --- Environment ---
python-dotenv==1.1.1

# --- Analysis outputs ---
matplotlib==3.8.4
pillow
reportlab==4.2.0

# --- Utilities ---
attrs==25.4.0
certifi==2026.2.25
packaging==26.0
simplejson==3.20.2
six==1.17.0
colorama==0.4.6
click==8.3.1
pyparsing==3.3.2

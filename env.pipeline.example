# PADIS pipeline Docker environment example.
# Copy this file to .env.pipeline and fill values for the target database.
# .env.pipeline is ignored by git through the existing .env.* rule.

DATABASE_URL=postgresql://user:password@host:5432/database
